{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "Default permissions capability for the application window",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
